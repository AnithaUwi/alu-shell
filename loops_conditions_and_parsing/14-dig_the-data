#!/usr/bin/env bash
# Use awk to parse the Apache log file, group by IP and HTTP code, and count occurrences
awk '{print $1, $9}' apache-access.log | 
	awk '{count[$1, $2]++} END {for (key in count) {print count[key], key}}' | 
	sort -nr | 
	awk '{print $1, $2, $3}'

